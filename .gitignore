# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# OS / editor
.DS_Store
*.swp

# LaTeX build artifacts (keep the .tex and .pdf, drop the rest)
*.aux
*.log
*.out
*.fls
*.fdb_latexmk
*.spl
*.synctex.gz
*.bbl
*.blg
*.toc

# Local virtual environments
.venv/
venv/

# Internal-only material not part of the public release
# (docs/ is now curated for publication -- see docs/ for the included
# protocol/decision-record/freeze-manifest files; reviewer correspondence
# and pre-review drafting notes are never copied into this repo at all)
g_refs/
artifacts/

# Local release-staging scratch (raw result directories, archives before
# upload); these are published separately as GitHub Release assets, not
# committed to git history
release_staging/
*.tar.zst
